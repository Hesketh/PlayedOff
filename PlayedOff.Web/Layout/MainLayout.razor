@inherits LayoutComponentBase
@using PlayedOff.Web.Auth;
@using PlayedOff.Web.Branding

<MudThemeProvider @ref="_mudThemeProvider" @bind-IsDarkMode="DarkMode" Theme="@Theming.Theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1">
        <MudToggleIconButton @bind-Toggled="DrawerOpen"
                             Icon="@Icons.Material.Filled.Menu"
                             ToggledIcon="@Icons.Material.Filled.MenuOpen"
                             Color="Color.Inherit"
                             ToggledColor="Color.Inherit"/>
        <MudSpacer />
        <MudImage Src="logo.svg" Alt="Mascot" Style="height:75%"></MudImage>
        <MudText Typo="Typo.h3" Class="ml-4">PLAYED OFF</MudText>
        <MudSpacer/>
        <LoginDisplay/>
    </MudAppBar>
    <MudDrawer @bind-Open="DrawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        <NavMenu />
    </MudDrawer>
    <MudMainContent Class="mt-8 px-8">
        @Body
    </MudMainContent>
</MudLayout>